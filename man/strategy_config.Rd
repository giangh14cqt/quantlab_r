% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy_config.R
\name{strategy_config}
\alias{strategy_config}
\title{Create a Validated StrategyConfig Object}
\usage{
strategy_config(
  strategy_type = "DCA",
  lookback = 252L,
  stop_loss = 0,
  initial_cash = 10000,
  dca_interval = 21L,
  invest_amount = 0,
  dip_pct = 0.05
)
}
\arguments{
\item{strategy_type}{Character. One of \code{"ATH"}, \code{"DCA"},
\code{"Dip"}.}

\item{lookback}{Integer. Lookback window in days (ATH only; default 252).}

\item{stop_loss}{Numeric. Stop-loss fraction, e.g. \code{0.10} for 10\%;
\code{0} disables it (default).  Active in ATH all-in mode only.}

\item{initial_cash}{Numeric. Starting capital (default 10 000).}

\item{dca_interval}{Integer. Days between DCA purchases (default 21).}

\item{invest_amount}{Numeric. Fixed investment per signal (DCA / Dip;
default 0 = all-in mode).}

\item{dip_pct}{Numeric. Drop fraction to trigger Dip buy (default 0.05).}
}
\value{
A validated \code{\linkS4class{StrategyConfig}} object.
}
\description{
Convenience constructor wrapping \code{new("StrategyConfig", ...)} and
immediately calling \code{validObject()}.  Unused slots receive sensible
defaults — only slots relevant to the chosen strategy are validated.
}
